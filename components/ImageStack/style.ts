import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    position: "absolute",
    top: 1,
    width: 50,
    height: 50,
    borderRadius: 25,
    borderColor: "#fff",
    borderWidth: 3,
    justifyContent: "center",
    alignItems: "center",
    marginLeft: 15,
    marginBottom: 15,
  },
});
